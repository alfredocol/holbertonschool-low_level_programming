#include "holberton.h"
#include <stdio.h>

/**
 * main - prints program name
 * @argc: # of arguments
 * @argv: array of strings
 *
 * Return: Always 0
 */
int	main(int argc, char *argv[])
{
	(void)argc;
	(void)argv;
	printf("%s\n", argv[0]);
	return (0);
}
